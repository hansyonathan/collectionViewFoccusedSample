# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'collectionViewFoccusedSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for collectionViewFoccusedSample
  pod 'MaterialControls', '~> 1.2.2'
  pod 'DZNEmptyDataSet'
  pod 'SDWebImage/WebP'
  
  target 'collectionViewFoccusedSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'collectionViewFoccusedSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
